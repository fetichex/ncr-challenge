import styled from 'styled-components'
import { Container } from '../AccountContainer/accountContainer.style'

export const ContainerInfo = styled(Container)`
  .info {
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
  }
`
